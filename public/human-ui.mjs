const main=document.querySelector('main');
const tabs=document.querySelector('.tabs');
const human=document.createElement('section');
human.id='human';
human.className='tool';
human.hidden=true;
human.innerHTML=`<div class="section-top"><div><p class="eyebrow">06 / HUMAN QA</p><h2>Keep the voice.<br><em>Check the edges.</em></h2></div><p>A small editorial screen before a public description, release note, or post.</p></div><label>What is this text for? <input id="human-context" maxlength="2000" placeholder="e.g. portfolio project description"></label><label>Public draft text<textarea id="human-text" rows="9" maxlength="16000" placeholder="Paste only text you are allowed to send to TypeSafe."></textarea></label><div class="toolbar"><button id="human-local">Run local pre-check</button><button id="export-human">Download for Jev screen</button></div><p id="human-status" role="status"></p><details open><summary>What it does, and what it does not</summary><pre>npm run human -- text-review.json</pre><p>The local pre-check finds simple issues such as empty drafts and excessive length. The optional local Jev run asks four bounded questions at once: unsupported claims, vague hype, missing limits and generic voice. It flags material to review, then leaves the rewrite to you. It cannot tell who wrote a text, prove it is “human”, or verify facts. Use public material only.</p></details>`;
main.insertBefore(human,main.querySelector('footer'));
const link=document.createElement('a');link.href='#human';link.textContent='06 Human QA';tabs.append(link);
function updateRoute(){const active=location.hash.slice(1)==='human';human.hidden=!active;if(active){document.querySelectorAll('.tool:not(#human)').forEach(node=>node.hidden=true);document.querySelectorAll('.tabs a').forEach(node=>node.toggleAttribute('aria-current',node===link));}}
addEventListener('hashchange',updateRoute);queueMicrotask(updateRoute);
function download(name,value){const url=URL.createObjectURL(new Blob([JSON.stringify(value,null,2)],{type:'application/json'}));const anchor=document.createElement('a');anchor.href=url;anchor.download=name;anchor.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
function localCheck(text){const problems=[];if(!text.trim())problems.push('Add a draft first.');if(text.length>5000)problems.push('Long draft: split it before review so each claim stays clear.');if(/\b(best|revolutionary|game-changing|insane)\b/i.test(text))problems.push('Potential hype word: make sure it earns its place with a concrete detail.');if(!problems.length)problems.push('No simple local issue found. This is not approval or a fact check.');return problems;}
document.querySelector('#human-local').onclick=()=>{document.querySelector('#human-status').textContent=localCheck(document.querySelector('#human-text').value).join(' ');};
document.querySelector('#export-human').onclick=()=>{const text=document.querySelector('#human-text').value,context=document.querySelector('#human-context').value;if(!text.trim()){document.querySelector('#human-status').textContent='Add a public draft first.';return;}download('text-review.json',{text,context:context||undefined});document.querySelector('#human-status').textContent='Config downloaded. Run the optional Jev screen locally, then import the report in Bench.';};
