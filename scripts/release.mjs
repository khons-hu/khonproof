import {mkdir,writeFile} from 'node:fs/promises';
const sites=[['Portfolio','https://khons-hu.vercel.app/',['Patrick','projects']],['Khonsolve','https://thinkroom-khonsu.vercel.app/',['Khonsolve','viewport']],['Khonrelay','https://quiet-signal-khonsu.vercel.app/',['Khonrelay','viewport']],['Khonodds','https://market-watch-khonsu.vercel.app/',['Khonodds','viewport']],['Dots','https://dots-khonsu.vercel.app/',['viewport']],['Khonproof','https://khonproof.vercel.app/',['Khonproof','viewport']]];const runs=[];
for(const [label,url,needles]of sites){const start=performance.now();try{const response=await fetch(url,{signal:AbortSignal.timeout(15000)});const html=await response.text();const checks={http:response.ok,html:response.headers.get('content-type')?.includes('text/html')===true,markers:needles.every(x=>html.toLowerCase().includes(x.toLowerCase())),viewport:/name=["']viewport["']/i.test(html)};runs.push({label,method:'HTTP + HTML smoke',url,passed:Object.values(checks).every(Boolean),checks,status:response.status,elapsedMs:Math.round(performance.now()-start)});}catch(e){runs.push({label,method:'HTTP + HTML smoke',url,passed:false,status:'error',error:e.message,elapsedMs:Math.round(performance.now()-start)});}}
await mkdir('reports',{recursive:true});await writeFile('reports/release.json',JSON.stringify({version:1,kind:'release-check',createdAt:new Date().toISOString(),scope:'HTTP and document checks only. Does not verify rendered layout, iOS, forms, or browser interactions.',runs},null,2));console.log(JSON.stringify(runs.map(({label,passed,status})=>({label,passed,status}))));if(runs.some(x=>!x.passed))process.exitCode=1;
